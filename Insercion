def insercion(A):
    for i in range(1, len(A)):
        n = A[i]
        j = i-1
        while j >= 0 and n < A[j]:
            A[j+1] = A[j]
            j -= 1
        A[j+1] = n

A = [64, 34, 25, 12, 22, 11, 90]
print("Lista no ordenada:", A)
insercion(A)
print("Lista ordenada:", A)
